# Stock Agents Monitor - 环境变量配置模板
# 复制此文件为 .env 并填写您的配置
# 最后更新: 2026-01-28

# ==============================================================================
# 环境设置
# ==============================================================================
ENV=development                     # development | production

# ==============================================================================
# LLM API Keys (至少配置一个)
# ==============================================================================
OPENAI_API_KEY=sk-...               # OpenAI API 密钥
ANTHROPIC_API_KEY=sk-ant-...        # Anthropic Claude API 密钥
GOOGLE_API_KEY=AIza...              # Google Gemini API 密钥

# ==============================================================================
# 数据源 API Keys (可选，用于增强数据获取)
# ==============================================================================
ALPHA_VANTAGE_API_KEY=              # Alpha Vantage 金融数据 API
FRED_API_KEY=                       # 美联储经济数据 API (FRED)
FINNHUB_API_KEY=                    # Finnhub 股票新闻 API

# ==============================================================================
# 数据库配置
# ==============================================================================
DATABASE_MODE=sqlite                # sqlite | postgresql

# SQLite 配置 (DATABASE_MODE=sqlite 时使用)
DATABASE_URL=sqlite:///./db/trading.db

# PostgreSQL 配置 (DATABASE_MODE=postgresql 时使用)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=trading

# ==============================================================================
# 安全配置
# ==============================================================================
API_KEY=                            # Admin API 访问密钥
API_KEY_ENABLED=false               # 是否启用 API Key 认证 (生产环境建议 true)
CORS_ORIGINS=http://localhost:3000  # 允许的跨域来源 (逗号分隔)

# ==============================================================================
# AI 配置加密密钥 (可选)
# ==============================================================================
# 用于加密存储在数据库中的 AI Provider API 密钥
# 如不设置，系统将自动生成并存储在 ./db/.encryption_key 文件中
AI_CONFIG_ENCRYPTION_KEY=           # 32 字节 base64 编码的 Fernet 密钥

# ==============================================================================
# 调度器配置
# ==============================================================================
DAILY_ANALYSIS_ENABLED=false        # 启用每日自动分析
DAILY_ANALYSIS_HOUR=9               # 每日分析执行小时 (0-23)
DAILY_ANALYSIS_MINUTE=30            # 每日分析执行分钟 (0-59)

# ==============================================================================
# 缓存配置 (可选)
# ==============================================================================
REDIS_URL=                          # Redis 连接 URL (未设置时使用内存缓存)
                                    # 格式: redis://localhost:6379/0

# ==============================================================================
# 存储路径
# ==============================================================================
CHROMA_DB_PATH=./db/chroma          # ChromaDB 向量数据库路径
PROMPTS_YAML_PATH=./config/prompts.yaml  # Prompt 配置文件路径
