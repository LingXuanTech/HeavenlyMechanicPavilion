
# HeavenlyMechanicPavilion (TradingAgents) 项目梳理报告

## 📋 项目概述

**TradingAgents** 是一个基于多智能体 LLM 的金融交易框架，使用 LangGraph 协调专业化的 AI 智能体团队（分析师、研究员、交易员、风险管理员）来研究市场并提出风险感知的交易决策。

### 核心特征
- 🤖 **多智能体协作**: 12+ 专业角色的 LLM 智能体
- 🔌 **可扩展插件系统**: 智能体和数据供应商热插拔
- 📊 **实时流式处理**: SSE/WebSocket 实时数据推送
- 🏗️ **Monorepo 架构**: PNPM workspace 管理三个包
- 🐳 **容器化部署**: Docker Compose 完整栈支持
- 📈 **回测系统**: 历史数据策略验证
- 🔐 **企业级安全**: JWT 认证、API 密钥管理

---

## 🏛️ 技术架构

### 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户界面层                                 │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │  Next.js Web UI  │  │   CLI 工具       │  │  API 客户端  │  │
│  └────────┬─────────┘  └────────┬─────────┘  └──────┬───────┘  │
└───────────┼────────────────────┼───────────────────┼───────────┘
            │                    │                   │
┌───────────┼────────────────────┼───────────────────┼───────────┐
│           │         API 网关层 (FastAPI)           │           │
│  ┌────────▼─────────────────────────────────────────▼────────┐ │
│  │  REST API  │  SSE Stream  │  WebSocket  │  Admin API     │ │
│  └────────┬───────────┬───────────┬──────────────┬───────────┘ │
└───────────┼───────────┼───────────┼──────────────┼─────────────┘
            │           │           │              │
┌───────────┼───────────┼───────────┼──────────────┼─────────────┐
│           │      业务服务层 (Services)            │             │
│  ┌────────▼─────────┐  ┌──────────▼──────┐  ┌───▼──────────┐  │
│  │  Graph Service   │  │ Execution Svc   │  │  Risk Mgmt   │  │
│  │  (LangGraph)     │  │                 │  │              │  │
│  └────────┬─────────┘  └──────────┬──────┘  └───┬──────────┘  │
│           │                       │              │             │
│  ┌────────▼─────────┐  ┌──────────▼──────┐  ┌───▼──────────┐  │
│  │  Market Data     │  │ Session Manager │  │  Agent LLM   │  │
│  │  Service         │  │                 │  │  Service     │  │
│  └────────┬─────────┘  └──────────┬──────┘  └───┬──────────┘  │
└───────────┼────────────────────────┼──────────────┼─────────────┘
            │                        │              │
┌───────────┼────────────────────────┼──────────────┼─────────────┐
│           │      智能体编排层 (LangGraph)         │             │
│  ┌────────▼─────────────────────────────────────────────────┐  │
│  │                  TradingAgentsGraph                       │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │  │
│  │  │ Analysts │→│Researchers│→│  Trader  │→│ Risk Mgr │ │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │  │
│  └───────────────────────────────────────────────────────────┘  │
└───────────┬────────────────────────┬──────────────┬─────────────┘
            │                        │              │
┌───────────┼────────────────────────┼──────────────┼─────────────┐
│           │      插件生态系统                     │             │
│  ┌────────▼─────────┐  ┌──────────▼──────┐  ┌───▼──────────┐  │
│  │  Agent Plugins   │  │ Vendor Plugins  │  │ LLM Providers│  │
│  │  (12+ roles)     │  │ (8+ sources)    │  │ (5 providers)│  │
│  └──────────────────┘  └─────────────────┘  └──────────────┘  │
└───────────┬────────────────────────┬──────────────┬─────────────┘
            │                        │              │
┌───────────┼────────────────────────┼──────────────┼─────────────┐
│           │         数据持久化层                  │             │
│  ┌────────▼─────────┐  ┌──────────▼──────┐  ┌───▼──────────┐  │
│  │   PostgreSQL     │  │     Redis       │  │   ChromaDB   │  │
│  │  (主数据存储)     │  │  (缓存/Pub/Sub) │  │  (向量记忆)  │  │
│  └──────────────────┘  └─────────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

### 数据流架构 

sequenceDiagram
    participant User
    participant API
    participant GraphService
    participant Agents
    participant MarketData
    participant LLM
    participant DB
    participant Redis
    
    User->>API: POST /sessions (创建会话)
    API->>DB: 创建 AnalysisSession
    API->>GraphService: 启动 LangGraph
    
    GraphService->>Agents: 执行分析师节点
    Agents->>MarketData: 请求市场数据
    MarketData->>Redis: 检查缓存
    MarketData->>Vendors: 拉取数据
    Vendors-->>MarketData: 返回数据
    MarketData->>Redis: 更新缓存
    MarketData-->>Agents: 返回数据
    
    Agents->>LLM: 调用 LLM 分析
    LLM-->>Agents: 返回分析结果
    
    GraphService->>Redis: 发布事件到 Pub/Sub
    Redis-->>User: SSE 流推送事件
    
    GraphService->>DB: 更新会话状态
    GraphService-->>API: 返回结果
    API-->>User: 返回会话摘要

## 📦 项目结构详解

### Monorepo 包组织

```
HeavenlyMechanicPavilion/
├── packages/
│   ├── backend/                    # Python 后端 (FastAPI + LangGraph)
│   │   ├── src/
│   │   │   └── tradingagents/      # 核心框架代码
│   │   │       ├── agents/         # 智能体插件系统
│   │   │       │   ├── analysts/   # 分析师智能体
│   │   │       │   ├── researchers/# 研究员智能体
│   │   │       │   ├── trader/     # 交易员智能体
│   │   │       │   ├── risk_mgmt/  # 风险管理智能体
│   │   │       │   ├── plugin_base.py    # 插件基类
│   │   │       │   └── plugin_registry.py# 插件注册表
│   │   │       │
│   │   │       ├── graph/          # LangGraph 状态机编排
│   │   │       │   └── trading_graph.py  # 主工作流
│   │   │       │
│   │   │       ├── llm_providers/  # LLM 提供商抽象层
│   │   │       │   ├── base.py     # 基础接口
│   │   │       │   ├── factory.py  # 工厂模式
│   │   │       │   ├── registry.py # 提供商注册表
│   │   │       │   ├── openai_provider.py
│   │   │       │   ├── claude_provider.py
│   │   │       │   └── deepseek_provider.py
│   │   │       │
│   │   │       └── plugins/        # 数据供应商插件
│   │   │           ├── base.py     # 供应商基类
│   │   │           ├── registry.py # 供应商注册表
│   │   │           ├── router.py   # 路由和回退
│   │   │           ├── config_manager.py # 配置管理
│   │   │           └── vendors/    # 内置供应商实现
│   │   │
│   │   ├── app/                    # FastAPI 应用层
│   │   │   ├── api/                # REST API 端点
│   │   │   │   ├── sessions.py    # 会话管理
│   │   │   │   ├── streams.py     # 事件流
│   │   │   │   ├── agents.py      # 智能体配置
│   │   │   │   ├── vendors.py     # 供应商管理
│   │   │   │   ├── llm_providers.py # LLM 提供商
│   │   │   │   ├── trading.py     # 交易操作
│   │   │   │   ├── backtests.py   # 回测
│   │   │   │   └── monitoring.py  # 监控
│   │   │   │
│   │   │   ├── services/           # 业务逻辑层
│   │   │   │   ├── graph.py       # Graph 服务
│   │   │   │   ├── events.py      # 事件管理
│   │   │   │   ├── execution.py   # 执行服务
│   │   │   │   ├── risk_management.py
│   │   │   │   ├── market_data.py
│   │   │   │   └── broker_adapter.py
│   │   │   │
│   │   │   ├── db/                 # 数据层
│   │   │   │   ├── models/        # SQLModel 数据模型
│   │   │   │   │   ├── analysis_session.py
│   │   │   │   │   ├── agent_config.py
│   │   │   │   │   ├── portfolio.py
│   │   │   │   │   ├── position.py
│   │   │   │   │   └── trade.py
│   │   │   │   └── database.py    # 数据库管理器
│   │   │   │
│   │   │   ├── repositories/       # 数据访问层
│   │   │   │   ├── base.py        # 基础仓储
│   │   │   │   └── *.py           # 各模型仓储
│   │   │   │
│   │   │   ├── cache/              # 缓存层
│   │   │   │   ├── cache_service.py
│   │   │   │   └── redis_client.py
│   │   │   │
│   │   │   ├── workers/            # 后台工作器
│   │   │   │   ├── data_worker.py
│   │   │   │   └── watchdog.py
│   │   │   │
│   │   │   ├── middleware/         # 中间件
│   │   │   ├── security/           # 安全模块
│   │   │   └── schemas/            # Pydantic schemas
│   │   │
│   │   ├── tests/                  # 测试套件
│   │   │   ├── unit/              # 单元测试
│   │   │   └── integration/       # 集成测试
│   │   │
│   │   ├── alembic/                # 数据库迁移
│   │   ├── pyproject.toml          # Python 项目配置
│   │   └── Dockerfile              # 容器镜像
│   │
│   ├── frontend/                   # Next.js 前端
│   │   ├── src/
│   │   │   ├── app/                # Next.js 13+ App Router
│   │   │   │   ├── page.tsx       # 主页
│   │   │   │   ├── layout.tsx     # 根布局
│   │   │   │   ├── dashboard/     # 仪表盘
│   │   │   │   ├── sessions/      # 会话详情
│   │   │   │   ├── admin/         # 管理界面
│   │   │   │   └── monitoring/    # 监控页面
│   │   │   │
│   │   │   ├── components/         # React 组件
│   │   │   │   ├── dashboard/     # 仪表盘组件
│   │   │   │   ├── sessions/      # 会话组件
│   │   │   │   ├── layout/        # 布局组件
│   │   │   │   └── ui/            # UI 基础组件 (shadcn)
│   │   │   │
│   │   │   ├── hooks/              # 自定义 Hooks
│   │   │   │   └── use-session-stream.ts
│   │   │   │
│   │   │   └── lib/                # 工具函数
│   │   │       ├── api/           # API 客户端
│   │   │       ├── store/         # Zustand 状态
│   │   │       └── utils.ts       # 工具函数
│   │   │
│   │   ├── package.json
│   │   └── Dockerfile
│   │
│   └── shared/                     # TypeScript 共享库
│       └── src/
│           ├── domain/             # 域模型和 DTOs
│           │   ├── session.ts     # 会话 DTOs
│           │   └── trading.ts     # 交易 DTOs
│           │
│           ├── clients/            # 客户端库
│           │   ├── http-client.ts
│           │   └── sse-client.ts
│           │
│           ├── utils/              # 工具函数
│           └── theme/              # UI 主题令牌
│
├── docs/                           # 完整文档
│   ├── ARCHITECTURE.md            # 架构文档
│   ├── API.md                     # API 文档
│   ├── SETUP.md                   # 安装指南
│   ├── DEPLOYMENT.md              # 部署指南
│   ├── DEVELOPMENT.md             # 开发指南
│   └── CONFIGURATION.md           # 配置指南
│
├── scripts/                        # 运维脚本
│   ├── deploy.sh                  # 部署脚本
│   └── healthcheck.sh             # 健康检查
│
├── docker-compose.yml              # 开发环境
├── docker-compose.prod.yml         # 生产环境
├── .env.example                    # 环境变量模板
└── MODULE_ANALYSIS_AND_TASKS.md   # 模块分析文档
```

---

## 🔧 技术栈详解

### 后端技术栈

#### 核心框架
| 技术 | 版本 | 用途 |
|------|------|------|
| **Python** | 3.10+ | 核心开发语言 |
| **FastAPI** | 0.115+ | 高性能 Web 框架 |
| **LangGraph** | 0.4.8+ | AI 智能体编排框架 |
| **LangChain** | 0.3+ | LLM 应用框架 |
| **Uvicorn** | 0.30+ | ASGI 服务器 |

#### 数据存储
| 技术 | 版本 | 用途 |
|------|------|------|
| **PostgreSQL** | 18 | 主数据库 